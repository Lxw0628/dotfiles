export LANG=zh_CN.UTF-8
export LANGUAGE=zh_CN:en_US

xrdb -merge ~/.Xresources

if [ -d /etc/X11/xinit/xinitrc.d ] ; then
 for f in /etc/X11/xinit/xinitrc.d/?*.sh ; do
  [ -x "$f" ] && . "$f"
 done
 unset f
fi

export EDITOR=nvim

prime-offload &

exec ~/.config/dwm/run.sh
